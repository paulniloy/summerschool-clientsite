import React from 'react';
import title from '../../Hooks/title';

const Dash = () => {
    title("Dashboard")
    return (
        <div>
            this is the dashboard
        </div>
    );
};

export default Dash;