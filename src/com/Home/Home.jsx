import React from 'react';
import title from '../../Hooks/title';

const Home = () => {
    title("Home")
    return (
        <div>
            this is home
        </div>
    );
};

export default Home;